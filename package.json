{
  "name": "weather-monitoring-api",
  "version": "1.0.0",
  "description": "REST API service for a weather monitoring system",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "prisma": "docker-compose run --rm prisma prisma",
    "migrate:dev": "docker-compose run --rm prisma prisma migrate dev",
    "migrate:deploy": "docker-compose run --rm prisma prisma migrate deploy",
    "migrate:create": "docker-compose run --rm prisma prisma migrate dev --create-only",
    "build": "tsc",
    "dev": "ts-node-dev src/server.ts",
    "test": "jest"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^2.19.0",
    "@prisma/client": "^5.16.1",
    "express": "^4.17.1",
    "pg": "^8.7.1",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/jest": "^27.0.3",
    "@types/node": "^16.11.12",
    "@types/uuid": "^8.3.3",
    "jest": "^27.4.5",
    "prisma": "^5.16.1",
    "ts-jest": "^27.1.1",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.3"
  }
}
